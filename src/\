import random as rnd
import numpy as np
import pandas as pd
import re
with open('./cross_validation/results.csv','r') as file:
    f = file 
    for line in f:
        if line == "filename;precision;recall;f1;novelty;coverage;rmse;mae":
            continue
        splited_line = line.split(",")

        splited_name = re.split(r'_|\.',splited_line[0],flags=re.MULTILINE | re.DOTALL)
        number_of_iteration = splited_name[len(splited_name)-2]
        print(type(number_of_iteration))
        if (number_of_iteration) == 0:
            print(number_of_iteration)
            continue
        

